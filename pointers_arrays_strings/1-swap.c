#include "main.h"

/**
 * 1-swap.c - Takes a pointer to an int as parameter and
 *               updates the value it points to to 98
 *
 * @n: Pointer to an integer
 *
 * Return: void
 */
void swap_int(int *a, int *b)
{
	*a = 98;
	*b = 402;
}
