#include <stdio.h>

/**
 * main - Prints all letters of alphabet in lowercase if conditions are met
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char sl;

	if (sl == 'e' && sl == 'q')
	{
		for (sl = 'a'; sl <= 'z'; sl++)
		{
			putchar(sl);
		}
	}

	putchar('\n');

	return (0);
}
