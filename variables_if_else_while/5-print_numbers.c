#include <stdio.h>

/**
 * main - Prints all letters of alphabet in lowercase if conditions are met
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 10; a++)
	{
	       printf("%d", a);
	}

	putchar('\n');

	return (0);
}
